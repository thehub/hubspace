to = "${BOOKING_CONTACT}"
sender = "noreply@the-hub.net"
subject = "HubSpace Alert: Tentative booking cancellation"
body = """\
Dear Host,

The booking for "${RESOURCE}" by user "${BOOKED_BY}" was just cancelled/expired. Therefore the resource would is available for booking from ${BOOKING_START} to ${BOOKING_END} on ${BOOKING_DATE}.

Tentative Booking details:
    Booked by:  ${BOOKED_BY}
    Resource:   ${RESOURCE}
    Start Time: ${BOOKING_START}
    End Time:   ${BOOKING_END}

This is an automated alert. Do not reply.

All the best,

The Hub+ Team
"""
